# MLP Experiments

This directory contains baseline grokking experiments with a simple MLP on modular arithmetic tasks.

The main goal is to study delayed generalization in a non-transformer setting and compare training dynamics against transformer models.
